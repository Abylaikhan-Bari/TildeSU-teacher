import 'package:flutter/material.dart';

class TrueOrFalseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16.0),
      child: Text('True/False Admin Screen'),
    );
  }
}
