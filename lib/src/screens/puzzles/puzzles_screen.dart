import 'package:flutter/material.dart';

class PuzzlesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16.0),
      child: Text('Puzzles Admin Screen'),
    );
  }
}
